[phases.setup]
nixPkgs = ["python311", "nodejs_20"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "cd web && npm install && cd .."
]

[phases.build]
cmds = ["cd web && npm run build && cd .."]

[start]
cmd = "uvicorn server:app --host 0.0.0.0 --port ${PORT:-8080}"
